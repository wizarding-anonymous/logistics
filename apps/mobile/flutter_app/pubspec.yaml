name: marketplace_mobile
description: A new Flutter project for the Logistics Marketplace.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # For making HTTP requests
  http: ^1.1.0

  # For securely storing tokens
  flutter_secure_storage: ^9.0.0

  # For state management
  provider: ^6.0.5

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
